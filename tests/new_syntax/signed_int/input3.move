module test {
    fun main() {
    while ({
        // test: Exp_::Quant(_, bind_range, ..) has bin_op
                spec {
                    invariant forall j in (i - amount)..bitvector.length:
                        ();
                    invariant forall k in 0..i - amount:
                        ();
                };
            }) {
            };

    }
}
        